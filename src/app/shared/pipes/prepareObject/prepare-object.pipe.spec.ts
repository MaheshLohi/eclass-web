import { PrepareObjectPipe } from './prepare-object.pipe';

describe('PrepareObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new PrepareObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
