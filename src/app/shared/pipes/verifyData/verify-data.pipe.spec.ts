import { VerifyDataPipe } from './verify-data.pipe';

describe('VerifyDataPipe', () => {
  it('create an instance', () => {
    const pipe = new VerifyDataPipe();
    expect(pipe).toBeTruthy();
  });
});
